<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical"
    tools:context=".ListRequest_defult">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="11dp"
        android:background="@color/white"
        android:orientation="horizontal"
        android:padding="6dp"
        android:weightSum="2">

        <ImageView
            android:id="@+id/imag_back_defult"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/actionBarItemBackground"
            android:src="@drawable/ic_forward_black" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:width="0dp"
            android:fontFamily="@font/irsans"
            android:gravity="center_vertical"
            android:text="لیست درخواست های پیش فرض"
            android:textColor="@color/black_num"
            android:textSize="15dp"
            android:textStyle="normal" />
    </LinearLayout>


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_defult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="10dp"
        tools:listitem="@layout/list_card_request_defult" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:background="@color/blue_bg"
        android:fontFamily="@font/irsans"
        android:text="افزودن درخواست (پیش فرض)"
        android:textColor="@color/white"
        android:textSize="16dp" />


</LinearLayout>